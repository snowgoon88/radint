## The files that make this library
## set( SRC_FILES )

## add_library( RADINT_LIB ${SRC_FILES} )
## set_target_properties( RADINT_LIB PROPERTIES LINKER_LANGUAGE CXX)
## set_target_properties( RADINT_LIB PROPERTIES COMPILE_FLAGS "${PROJECT_ALL_CFLAGS} ${SHARE_PATH_CFLAGS}")
